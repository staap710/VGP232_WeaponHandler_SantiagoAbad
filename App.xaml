<Application x:Class="Assignment1.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Color palette -->
        <SolidColorBrush x:Key="PrimaryBackgroundBrush" Color="#FF1E1E1E"/> <!-- window background -->
        <SolidColorBrush x:Key="PrimaryForegroundBrush" Color="#FFFFFFFF"/> <!-- default text -->
        <SolidColorBrush x:Key="AccentBrush" Color="#FF007ACC"/> <!-- buttons, highlights -->
        <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="#FF2D2D30"/>
        <SolidColorBrush x:Key="ButtonForegroundBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="ListBackgroundBrush" Color="#FF252526"/>
        <SolidColorBrush x:Key="ListForegroundBrush" Color="#FFFFFFFF"/>

        <!-- Default Button style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <!-- Default ListBox style -->
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource ListBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ListForegroundBrush}"/>
        </Style>
    </Application.Resources>
</Application>
